root: {
  shared = root + "/shared";
  hardware = root + "/hosts/hardware";
  modules = root + "/system-module-configuration";
  homeManager = root + "/home-manager";
  profileDir = root + "/hosts/profile";
}
