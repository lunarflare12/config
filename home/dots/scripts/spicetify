#!/usr/bin/env bash
wrap="/etc/profiles/per-user/${USER}/bin/spicetify"
if [[ -x "$wrap" ]]; then
  eval "$(grep -E '^export (SPOTIFY_BIN|SPICETIFY_BIN)=' "$wrap" || true)"
fi
exec "${HOME}/.config/scripts/spotify.sh" --cli "$@"
